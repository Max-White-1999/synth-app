/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // reactCompiler: true,
  output: 'export',
  basePath: "/synth-app",
  distDir: 'docs',
};

export default nextConfig;
